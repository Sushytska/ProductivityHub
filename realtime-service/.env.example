# .NET API base URL — plain HTTP for local dev (avoids trusting the ASP.NET Core dev cert)
DOTNET_API_BASE_URL=http://localhost:5043

# Port this relay service listens on for Socket.IO connections
PORT=4000

# Allowed Socket.IO CORS origin — tighten once the Angular app's origin is known
CORS_ORIGIN=*
