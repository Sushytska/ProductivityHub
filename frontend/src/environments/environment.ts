export const environment = {
  // Empty = same-origin (production, served from the same Nginx origin as
  // /api and /socket.io). ChatSocket falls back to window.location.origin.
  realtimeUrl: '',
};
